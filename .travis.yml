language: java
jdk:
  - oraclejdk8
env:
  - PLAY_VERSION=1.3.7
before_script:
  - wget http://downloads.typesafe.com/typesafe-activator/${PLAY_VERSION}/typesafe-activator-${PLAY_VERSION}-minimal.zip
  - unzip -q typesafe-activator-${PLAY_VERSION}-minimal.zip
script: activator-dist-${PLAY_VERSION}/activator test
